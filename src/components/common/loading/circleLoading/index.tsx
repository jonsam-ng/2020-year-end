import React, { FC } from '../../../lock/node_modules/react';
import './index.scss';

const CircleLoading: FC = (props: any) => {
  return (
    <div className="cl--containder">
    </div>
  )
}

export default CircleLoading;